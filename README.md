# SocratesOS exact visual mirror

This directory preserves the deployed Poly Perps visual structure, spacing, typography, colors, sculptures, WebGL scene, transitions and responsive behavior. Only visible copy, metadata and product identity were changed for SocratesOS.

Run:

```bash
npm start
```

Open http://127.0.0.1:4173

The files are a compiled public-deployment mirror, not the original component source. The untouched download remains in `../reference-original/`. Confirm rights or replace third-party visual assets before public/commercial distribution.
